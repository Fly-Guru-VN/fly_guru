import { PageHeader } from "@/components/cabinet/PageHeader";

// Роль «агент» есть, а строки в agents нет. Так бывает у админа, заглянувшего
// в чужой кабинет, и у аккаунта, заведённого руками мимо формы «Новый агент».
// Показываем это словами: пустые нули читались бы как «я ничего не заработал».
export function NoAgentProfile() {
  return (
    <div>
      <PageHeader title="Кабинет агента" hint="Профиль агента не найден" />
      <p className="mt-4 text-sm text-muted">
        К этому аккаунту не привязана агентская ссылка, поэтому показывать
        нечего. Если вы агент школы — напишите начальнику, он заведёт ссылку в
        разделе «Агенты».
      </p>
    </div>
  );
}
